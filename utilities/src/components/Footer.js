import React from 'react'

export const Footer = () => {
  const style = {
    padding: "1rem",
    display: "grid",
    placeContent: "center",
    // position: "sticky",
    // bottom: "0"
  }

  return (
    <div style={style}>Footer</div>
  )
}